Installation:
Clone install_dots.sh: git clone https://github.com/gladendemon-ux/ma-dots/tree/main/install_dots.sh
Run the installer
Dependencies:
git(installer)
curl(if you're going to use the music widget)
